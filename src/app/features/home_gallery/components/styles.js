import {StyleSheet} from 'react-native';

const homeGalleryStyles = StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default homeGalleryStyles;
